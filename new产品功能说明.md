# 视频AI识别自进化能力平台产品功能说明

## 1. 文档说明

### 1.1 文档目的

本文档用于指导“视频AI识别自进化能力平台”2.3版本的产品、前端、后端、算法、测试和交付实施。文档描述系统当前应实现的功能，不记录原型修改过程。开发人员应以本文档的业务规则、字段定义、计算口径、交互规则、权限规则和异常处理要求为实现与验收依据。

### 1.2 系统目标

平台围绕“作业必须有视频、视频必须可回溯、风险必须可识别、问题必须可闭环、算法必须可持续提升”建设，形成以下业务闭环：

1. 维护组织、作业区域、班次、作业类型、设备和人员基础数据。
2. 创建作业计划并绑定三级作业区域与摄像头。
3. 实时巡检作业画面，基于作业、风险和AI预警规则自动排序播放。
4. 作业结束后获取录像，对有人片段进行预计算并支持人工倒查。
5. AI或人工形成违章、隐患单据，完成复核、查处、整改、验收和考核闭环。
6. 将复核结果沉淀为训练样本，跟踪算法版本和精准度提升趋势。
7. 通过统计分析和领导履职台账衡量风险发现、处置和履职效果。

### 1.3 角色定义

| 角色 | 主要职责 | 默认数据范围 |
|---|---|---|
| 集团管理员 | 配置全集团基础数据、查看全局统计、管理算法与算力 | 集团及全部下级单位 |
| 单位管理员 | 管理本单位区域、设备、人员、作业计划和台账 | 本单位及授权下级单位 |
| 安全管理人员 | AI复核、违章查处、隐患查处、统计分析 | 授权组织范围 |
| 作业计划人员 | 新建、编辑、发布作业计划，上传作业视频 | 本单位及被授权作业区域 |
| 监控人员 | 实时巡检、人工标注、视频倒查 | 授权摄像头和作业范围 |
| 领导/履职人员 | 视频倒查、发现问题、查看个人履职记录 | 本人记录及授权组织范围 |
| AI研发人员 | 管理算法、版本、训练样本和精准度数据 | 授权算法和脱敏样本范围 |
| 审计人员 | 只读查看关键台账、日志和统计口径 | 明确授权范围，只读 |

## 2. 全局业务与技术规则

### 2.1 数据权限

1. 所有列表、统计卡片、图表、导出和详情接口必须由后端按登录人的组织权限过滤，前端隐藏不能替代后端鉴权。
2. 组织权限采用“本人所属单位 + 被授权单位 + 是否包含下级”的组合模型。集团权限可以查看全集团；单位权限不得越级访问平级或上级单位数据。
3. 作业区域、摄像头、人员、作业计划和问题单据均必须携带 `org_id`。查询时以资源所属组织为基准校验访问权。
4. 操作权限至少拆分为查看、新增、编辑、删除、导入、导出、复核、查处、整改、验收、算法管理和算力管理。按钮可见性与接口权限必须一致。
5. 详情链接不得仅依赖前端列表来源。用户直接输入无权访问的ID时，接口返回403，页面提示“无权查看该数据”。
6. 导出沿用当前筛选条件和数据权限；导出任务记录操作人、时间、筛选条件、数据范围和文件摘要。
7. 手机号默认显示前三位和后四位，身份证号默认隐藏中间位。查看完整敏感信息需单独权限并记录审计日志。
8. AI研发中心获取的图片、录像和样本默认脱敏；若包含人脸或个人身份信息，必须获得业务授权并记录用途、使用范围和保留期限。

### 2.2 组织架构面板

1. 所有带组织架构树的页面默认收起，只显示位于内容区左侧边界垂直居中的“展开”按钮。
2. 点击“展开”后，组织树从左侧展开，主内容区等宽让位，不覆盖业务内容；按钮随面板移动到右边界垂直居中并显示“收起”。
3. 点击“收起”后恢复默认状态。展开状态可在当前会话中保持，刷新页面后恢复默认收起。
4. 选择组织节点后，刷新该页面所有列表、卡片和图表。父节点包含其下级数据；叶子节点只显示本节点数据。
5. 组织树节点显示加载、空数据和加载失败状态。加载失败时提供“重新加载”。

### 2.3 通用查询、表格和导出

1. 查询按钮根据当前条件重新请求数据并回到第1页；重置按钮恢复默认筛选并重新查询。
2. 关键词去除首尾空格；空字符串视为未筛选。日期区间要求开始时间不晚于结束时间。
3. 表格分页参数统一为 `page_no`、`page_size`，默认每页10条；总数由后端返回。
4. 排序必须传递排序字段和方向；未指定时按业务发生时间倒序、主键倒序，保证分页稳定。
5. 空数据展示空状态和清空筛选入口；接口失败展示错误提示和重试按钮；加载期间避免重复提交。
6. 导入先上传、解析和校验，再展示成功数、失败数及错误原因；用户确认后才写入正式数据。重复数据按业务唯一键提示，不静默覆盖。
7. 删除需二次确认。已被业务引用的数据原则上禁止物理删除，可停用或逻辑删除，并说明引用关系。

### 2.4 通用数值与公式口径

1. 百分比统一按 `分子 ÷ 分母 × 100%` 计算，四舍五入保留两位小数；分母为0时显示“--”。
2. 数量指标显示整数；金额保留两位小数；精准度保留两位小数。
3. 时长由秒换算为“X天X时X分”，不足1分钟显示“0天0时1分”；统计计算保留原始秒数，不用展示值反算。
4. 跨日业务日期展示为“开始日期 ～ 结束日期”；同日可仅显示单日。
5. 视频回传率 = 已完成回传的应回传作业数 ÷ 应回传作业数 × 100%。
6. 复核完成率 = 已复核预警数 ÷ 应复核预警数 × 100%。
7. 整改完成率 = 已验收通过隐患数 ÷ 应整改隐患数 × 100%。
8. 算法精准度 = 正确识别数 ÷（正确识别数 + 误报数）× 100%。
9. 算法提升幅度 = 本次训练后精准度 - 上一次有效版本精准度，单位为百分点。
10. 集团或跨单位统计必须先明确去重主键。涉及作业、预警、人员时分别以 `work_id`、`warning_id`、`person_id` 去重。

### 2.5 状态与审计

1. 状态变更由后端状态机控制，不接受前端任意传入目标状态。
2. 每次新增、编辑、删除、发布、复核、查处、整改、验收、导入、导出、手工绑定和自动关联均记录审计日志。
3. 审计日志至少包含资源类型、资源ID、原值、新值、操作人、组织、时间、来源IP和结果。
4. 所有时间存储为带时区时间戳，界面按用户时区展示；本项目默认使用中国标准时间。

## 3. 首页与综合态势

### 3.1 首页（`首页.html`、`index.html`）

**功能目标**：向当前用户展示其权限范围内的待办、预警、作业、设备和处置概览，并提供常用功能入口。

**核心字段与指标**

| 字段/指标 | 说明与口径 |
|---|---|
| 今日作业数 | 当日作业时间与今日有交集且状态非作废的作业计划数 |
| 危险作业数 | 今日作业中危险作业标识为“是”的去重作业数 |
| 无视频作业数 | 应绑定视频但未绑定有效摄像头，或应回传但无有效视频的作业数 |
| 实时预警数 | 当日产生且用户有权限查看的AI预警数 |
| 待复核数 | 复核状态为待复核的预警数 |
| 待处理违章/隐患 | 已确认且尚未完成业务闭环的单据数 |
| 设备在线率 | 在线摄像头数 ÷ 纳管且启用的摄像头数 × 100% |

**交互**

- 点击指标卡进入对应列表并携带组织、日期和状态筛选。
- 待办按优先级、截止时间和产生时间排序；点击进入可操作详情。
- 快捷入口仅展示用户有权限的菜单。
- 指标加载失败不影响其他模块，并在模块内提供重试。

### 3.2 监测一张图（`监测一张图.html`）

**功能目标**：在地图或矿区平面图上统一呈现作业区域、摄像头、实时作业、预警和问题分布。

**字段**：组织、区域、经纬度/图上坐标、摄像头类型、在线状态、当前作业、危险作业标识、预警类型、预警时间、处置状态。

**交互**

- 组织、时间、资源类型和状态共同过滤图层。
- 聚合点显示资源数量；放大后拆分具体点位。
- 点击摄像头打开预览卡，提供进入实时巡检和设备详情入口。
- 点击预警打开摘要，提供进入AI复核或查处详情入口。
- 无坐标资源进入“未定位数据”列表，不纳入地图点位统计。

## 4. 作业计划管理

### 4.1 作业计划列表（`作业计划管理.html`）

**功能目标**：维护作业计划、摄像头关联、视频回传及实时巡检/倒查入口。

**数据范围**：当前组织及授权下级单位的作业计划。组织树选择与筛选条件同时生效。

**筛选字段**

| 字段 | 类型 | 规则 |
|---|---|---|
| 作业日期 | 快捷日期/日期区间 | 默认今日；作业起止时间与区间有交集即命中 |
| 作业类型 | 级联选择 | 一级类型选择后加载二级分类 |
| 作业区域 | 三级区域树 | 选择父级时包含下级三级区域 |
| 回传状态 | 下拉 | 未回传、部分回传、已回传 |
| 是否危险作业 | 下拉 | 全部、是、否 |
| 关键词 | 输入框 | 匹配作业内容、位置、负责人和计划编号 |

**列表字段**：序号、视频回传状态、作业内容、摄像头、作业类型、二级分类、作业区域、作业位置、作业班次、作业日期、作业单位、所属单位、是否危险作业、状态、操作。

**状态规则**

- 草稿：可编辑、删除、发布。
- 已发布/进行中：可查看、查看监控、上传视频；关键字段修改需记录版本。
- 已完成：可详情、倒查、上传补充视频。
- 已作废：只读，不纳入正常统计。
- 回传状态按应回传摄像头数量计算：0路完成为未回传，部分完成为部分回传，全部完成为已回传。

**按钮与交互**

- “新建作业计划”：进入新增页面。
- “详情”：只读展示计划、人员、区域、摄像头、操作记录和视频状态。
- “编辑”：加载原数据，保存后写版本日志。
- “查看监控”：进入实时在线巡检的“作业计划”页签，只展示该作业；目标页显示“取消过滤/查看全部作业”和“返回”按钮。
- “倒查”：进入按作业计划倒查页，只展示该作业；同样提供取消过滤与返回。
- “上传视频”：打开右侧抽屉。先展示关联作业计划，再选择该作业已绑定的一台摄像头，最后选择视频文件。摄像头为必填，文件支持格式、大小和时长校验。
- 统计卡片只显示当前筛选范围内的数量，不显示环比或较昨日文案。

### 4.2 新增/编辑作业计划（`新增作业计划.html`）

**基础字段**

| 字段 | 必填 | 规则 |
|---|---:|---|
| 作业内容 | 是 | 2～100字，同单位同日期允许重名但编号唯一 |
| 作业类型/二级分类 | 是 | 级联选择，来源于启用的作业类型字典 |
| 作业区域 | 是 | 只能选择三级作业区域 |
| 作业位置 | 是 | 默认带出区域划分位置，可补充具体描述 |
| 作业日期 | 是 | 日期值 |
| 作业班次 | 是 | 带出班次起止时间；跨日班次正确计算结束日期 |
| 作业开始/结束时间 | 是 | 开始早于结束，可在班次范围内调整 |
| 作业单位/所属单位 | 是 | 受组织权限约束 |
| 作业负责人 | 是 | 从本单位有效人员中选择 |
| 作业人员 | 否 | 支持多选、移除和关键词搜索 |
| 是否危险作业 | 是 | 是/否；选择“是”时显示风险类型与管控措施 |
| 摄像头 | 是 | 至少一台有效摄像头，来源于所选三级区域 |

**交互与校验**

- 切换三级区域后清空已选摄像头并提示重新选择，防止跨区域错误绑定。
- 作业时间冲突时提示同一区域、同一摄像头的冲突计划，允许具备冲突确认权限的用户继续保存。
- 保存草稿只校验编号和关键关联；发布校验全部必填字段与至少一台摄像头。
- 编辑已开始作业时，不允许修改所属单位、作业日期和已产生录像的摄像头。

### 4.3 摄像头选择（`新增作业计划-选择摄像头.html`、`选择摄像头.html`）

**功能目标**：按组织、三级区域、类型和状态选择计划所需摄像头。

**字段**：摄像头名称、设备编码、类型（固定/移动/执法）、所属单位、三级区域完整路径、在线状态、当前占用计划、已选状态。

**交互**

- 默认仅展示所选三级区域下启用设备；可切换“显示其他授权区域”，但需确认跨区域绑定。
- 支持单选、批量多选、全选当前页和取消选择；跨页保留已选项。
- 已被时间冲突计划占用的设备显示冲突标识。
- 确认后返回已选设备ID及快照信息，父页面再次校验有效性。

### 4.4 无视频作业（`无视频作业.html`）

**功能目标**：识别应有视频但缺少有效视频证据的作业并推动补传。

**判定场景**：计划未绑定摄像头、绑定设备无录像、应回传数量未完成、上传文件校验失败或录像时间未覆盖作业时段。

**列表字段**：计划编号、作业内容、单位、三级区域、作业时间、应回传摄像头数、已回传数、缺失原因、责任人、超时时长、操作。

**交互**：“补充绑定”“上传视频”“查看计划”“催办”。催办记录接收人和结果，同一计划在配置间隔内防重复发送。

## 5. 作业区域与基础管理

### 5.1 作业区域管理（`作业区域管理.html`）

**功能目标**：维护严格的三级作业区域，并将摄像头、风险属性和实际空间位置绑定到第三级。

**层级规则**

| 层级 | 含义 | 可用操作 |
|---|---|---|
| 一级 | 大区/系统/采区 | 查看；不提供编辑、删除和新增下级按钮 |
| 二级 | 分区/楼栋/生产区域 | 新增作业区域（创建三级）；不提供编辑、删除 |
| 三级 | 具体作业区域 | 编辑、删除、查看/调整区域划分、查看关联摄像头 |

**列表字段**：序号、作业区域名称、层级、风险等级、区域负责人、所属单位、关联摄像头数量、操作。

**新增/编辑三级区域抽屉字段**

| 字段 | 必填 | 规则 |
|---|---:|---|
| 上级区域 | 是 | 新增时固定为发起操作的二级区域 |
| 区域名称 | 是 | 同一上级下唯一，2～50字 |
| 风险等级 | 是 | 取启用的风险等级；标签显示红色星号 |
| 区域负责人 | 否 | 从所属单位有效人员中选择 |
| 负责人手机号 | 否 | 选择人员后自动带出并脱敏展示 |
| 区域划分 | 是 | 位于抽屉最底部，点击选择框打开平面图弹窗 |

**区域划分弹窗**

- 先选择楼栋和楼层，再在平面图中单击房间或片区。
- 支持多选；已选区域高亮，底部按“楼栋/楼层/房间”展示全部已选项。
- 同一空间可按业务配置是否允许被多个三级区域复用；不允许时必须提示冲突区域。
- 点击“确认划分”回填选择框；保存三级区域时同时保存空间关联。

**摄像头关联**

- 表格只显示数量，如“2台”；点击数量打开右侧摄像头列表。
- 列表中摄像头左侧按固定、移动、执法设备显示不同图标，并展示名称、编码、在线状态。
- “自动关联设备”按摄像头空间坐标/房间归属将设备关联到三级区域。点击后按钮进入加载状态，完成后刷新数量并提示“自动关联摄像头成功”；失败时显示成功、失败明细并允许重试。

### 5.2 作业班次（`作业班次.html`）

**字段**：班次名称、开始时间、结束时间、是否跨日、所属单位、状态、备注。

**规则**：同单位班次名称唯一；结束时间小于或等于开始时间时视为次日；被作业引用的班次只能停用，不能物理删除。

### 5.3 作业类型管理（`作业类型管理.html`）

**字段**：一级类型、二级分类、编码、是否危险作业默认值、状态、排序、说明。

**规则**：编码全局唯一；二级分类必须隶属于一级类型；停用后历史数据正常展示，新建计划不可选择。

## 6. 设备与人员管理

### 6.1 设备管理列表（`设备管理列表.html`）

**功能目标**：查询摄像头基础信息、AI能力、所属三级区域和地图选点状态。

**筛选字段**：所属单位、摄像头类型、品牌、场景、在线状态、是否支持AI分析、名称/编码关键词。

**列表字段**：选择框、序号、摄像头名称、设备编码、摄像头类型、品牌、场景、所属单位、所属区域（完整三级路径）、覆盖区域示例、是否支持AI分析、在线状态、操作。列表不展示直播流地址和设备状态字段。

**交互**

- “添加摄像头”和“批量删除”在当前版本不显示，但接口与权限模型保留。
- “树状预览”入口当前不显示；`设备管理树状.html`作为兼容路由保留，不进入主菜单。
- “预览”打开视频预览；“编辑”打开抽屉；“已选点”表示已有坐标，点击可查看或调整点位；无坐标时显示“地图选点”；“删除”执行引用校验。
- 所属区域只能选择第三级，列表显示“一级 > 二级 > 三级”。

### 6.2 设备树状页（`设备管理树状.html`）

按组织、一级区域、二级区域、三级区域和设备形成树；叶子节点为摄像头。字段及操作与设备列表一致。此页面仅供兼容和后续启用，不在当前主入口显示。

### 6.3 人脸采集库（`人脸采集库.html`）

**功能目标**：维护可用于人员识别的人员档案、人脸照片和启用状态。

**筛选字段**：姓名/身份证/手机号关键词、状态、更新时间；组织树默认收起。

**列表字段**：序号、人员信息、证件信息、所属企业、人脸照片、状态、更新时间、操作。

**统计卡片**：人脸库总数、今日新增、已启用、未启用、特殊关注名单数量；卡片只展示数量，不展示占比或较昨日。

**交互与权限**

- “手机采集”生成短期二维码或链接，进入移动端采集。
- 查看完整证件号、人脸原图、删除人脸均需敏感数据权限。
- 特殊关注名单通过独立业务字段管理，普通列表行不显示“黑名单”标签。
- 同一证件只允许一个有效人员档案；人脸质量不达标时不得启用。

### 6.4 移动端人脸采集（`移动端人脸采集.html`）

**流程**：校验采集任务 → 阅读授权说明 → 填写/确认身份 → 活体检测 → 多角度拍摄 → 质量检测 → 提交。

**质量规则**：清晰度、亮度、遮挡、面部占比、活体结果均通过才可提交。失败给出明确原因与重拍指引。采集链接过期或已使用时不可重复提交。

## 7. 实时在线巡检

### 7.1 实时监控（`实时监控.html`）

**功能目标**：按作业区域或作业计划浏览实时视频，支持自动排序播放、手动选播、预警推送和人工标注。

**筛选与布局**

- 页签：作业区域、作业计划。
- 筛选：是否高危作业、作业类型、是否绑定作业计划；点击“查询”后生效。
- 宫格：1、4、9宫格，默认9宫格。9宫格按3×3铺满可用内容区域。
- 翻页器位于右下角、预警消息悬浮窗上方，显示上一页、数字页码、下一页。
- “自动排序播放”是外层开关；“自动播放设置”打开规则弹窗。

**自动排序规则**

1. 违章/隐患AI预警画面。
2. 正在进行的危险作业画面。
3. 正在进行的普通作业画面。
4. 重大风险、较大风险区域画面。
5. 人员长时间停留画面。

同级按事件发生时间倒序、摄像头编码升序。某高优先级事件结束后，重新按现有规则补位。

**手动选播规则**

- 第一次点击树中摄像头时，清空所有宫格，仅在第一个宫格播放该摄像头。
- 后续点击其他摄像头时，依次填入空闲宫格；已选摄像头再次点击则取消并释放宫格。
- 触发手动选择后自动关闭“自动排序播放”。
- 打开“自动排序播放”时清除全部手动选中状态，并按自动规则重新填充宫格。
- 选中数量达到当前宫格上限时，继续点击提示“当前宫格已满”。

**预警消息**

- 消息面板固定在页面底部。关闭后折叠为只显示标题和数量的悬浮表头，可再次展开。
- 新消息到达时自动展开并高亮；用户正在操作弹窗时不得抢占焦点。
- 演示模式：页面加载5秒后推送“人员长时间停留”，展示5秒后折叠；再间隔5秒推送“未佩戴安全帽”，展示5秒后折叠。生产环境由实时消息服务驱动。
- 点击消息定位并播放对应摄像头；“创建作业计划”“AI预警核查”“忽略”按消息类型显示。

**视频操作**：全屏、静音/音量、标注违章/隐患、拍照截图。截图生成带摄像头、时间和操作人水印的图片并进入标注表单图片区。

### 7.2 自动播放设置（`实时在线监控轮播规则弹窗.html`）

**字段**：自动播放开关、轮播间隔（15/20/30秒）、优先级规则顺序。

**规则**

- 支持拖拽调整五类规则顺序并保存用户级配置。
- “人员长时间停留”不设置独立停留时长，时长来自系统预警规则。
- 配置保存后立即对下一轮自动播放生效。
- 至少保留一条启用规则；关闭总开关时不执行自动轮播。

### 7.3 实时抓拍与人工标注（`AI预警实时和倒查标记.html`）

**左侧视频区**：显示摄像头、作业区域、关联作业和当前时间。控制栏中的“标注违章/隐患”为明确按钮，并提供“拍照截图”按钮。

**违章/隐患查处页签表单字段**

| 字段 | 必填 | 规则 |
|---|---:|---|
| 违章/隐患照片 | 是 | 可上传或使用截图，多图可删除和预览 |
| 单据类型 | 是 | 违章/隐患；切换后重置不兼容字段 |
| 违章名称/隐患描述 | 是 | 违章取启用规则，隐患填写描述并选择分类 |
| 等级 | 是 | 违章等级为只读匹配结果；隐患选择隐患等级 |
| 违章记分 | 条件必填 | 违章时按规则自动带出，只读 |
| 责任单位 | 是 | 受当前组织权限限制 |
| 条款/依据 | 否 | 按规则带出，可按权限补充 |
| 涉及人员 | 违章必填 | 标签形式展示，支持移除和“添加人员” |
| 发现时间 | 是 | 默认当前视频时间，可修改到录像覆盖范围内 |
| 作业位置 | 是 | 默认三级区域/作业位置 |

点击“确定提交”先做校验，再生成待处理业务单据；防重复规则为同摄像头、同名称、相近时间窗口提示疑似重复。

**我标注的页签**：字段包括缩略图、标注人、标注单位、内容、时间、单据类型、责任单位、处理状态和操作。已处理记录仅显示详情；待处理/待核实记录显示详情和删除；不提供编辑。删除仅允许标注人且尚未进入正式处置流程。

## 8. 事后视频倒查

### 8.1 视频倒查列表（`视频倒查列表页面.html`）

**功能目标**：从作业日历和作业事项进入录像回放，掌握各单位回传完整度。

**布局**：组织树默认收起；展开后位于日历左侧。右侧依次为作业日历、当日统计、单位回传进度、筛选区和作业事项列表。

**筛选字段**：作业位置、作业区域、视频回传状态、作业内容/作业单位关键词。关键词同时匹配作业内容和作业单位；不设置独立“作业单位/所属单位”筛选。

**列表字段**：序号、作业内容、作业位置、作业时间、作业单位、所属单位、三级作业区域、是否危险作业、二级分类、摄像头、视频回传状态、操作。

**计算**

- 日历绿点表示当日存在作业；选择日期后刷新全部统计和列表。
- 单位回传率 = 该单位已回传作业数 ÷ 该单位应回传作业数 × 100%。
- 部分回传显示完成路数/应回传路数。

### 8.2 按作业计划倒查（`视频倒查按作业计划.html`）

**功能目标**：选择作业和摄像头，查看录像、有人片段及人工标注结果。

**过滤场景**

- 从作业计划“倒查”进入时，按URL中的 `work_id` 仅展示目标作业，并显示“取消过滤/查看全部作业”和“返回”按钮。
- 点击取消过滤后展示权限范围内全部作业；返回回到来源页面并保留来源筛选。

**视频区**：关联摄像头卡片、录像画面、录像/有人/违章隐患时间轴、标注按钮、拍照截图按钮。控制区不提供下载视频、快捷时长和手工时间输入。

**列表规则**：作业按时间倒序；摄像头按绑定顺序；切换摄像头同步刷新视频和时间轴。无录像时展示缺失原因和补传入口。

**我标注的**：与实时标注页规则一致。字段统一使用“处理状态”；不展示复核结果字段和“我标记”标签。

## 9. AI预警复核

### 9.1 AI违章预警复核列表（`AI隐患核查.html`）

**功能目标**：查询AI预警并进入人工复核，列表字段“违章名称”表示触发的业务规则名称。

**筛选字段**：预警时间、违章名称、复核状态、违章等级、作业位置、关键词。组织树默认收起。

**统计指标**：预警总数、待复核、确认违章、误报（非违章）、复核完成率。

**列表字段**：序号、预警时间、违章名称、作业位置、摄像头、作业内容、作业单位、违章等级、AI置信度、复核状态、复核结果、操作。

**状态**：待复核 → 已复核；复核结果为确认违章或非违章。已复核记录不可直接再次修改，具备复核纠正权限时走更正流程并保留原结果。

### 9.2 AI违章预警复核详情（`AI识别的违章复核.html`）

**布局**：左侧视频与时间轴，右侧复核表单。表单内字段顺序如下：抓拍图片、违章名称、违章等级、违章单位、违章记分、条款依据、复核结果、违章人员、备注。

**字段规则**

- 违章名称位于违章等级上方，来源于触发规则。
- 违章等级、单位和记分按规则带出；具备权限时可修正，修正需填写原因。
- 复核结果位于违章人员上方，必填：确认违章/非违章。
- 违章人员采用标签样式，显示姓名和工号，支持移除和“添加人员”。
- 详情中的人员必须完整列出姓名，不使用“等N人”。
- 作业区域/位置旁使用标准描边按钮“查看位置”，点击打开地图或平面图定位。

**提交逻辑**

- 确认违章：生成或关联违章查处单，样本标记为正样本。
- 非违章：不生成查处单，记录误报原因，样本标记为负样本。
- 提交成功后刷新列表统计，不允许重复提交。

### 9.3 预警详情统一规范

所有AI预警详情的章节名称统一为“预警信息”“预警截图”“预警视频”“作业信息”“复核结果/处置信息”。“作业信息”展示作业类型、二级分类、作业内容、三级区域/位置、日期、作业单位、危险作业标识和所属单位。

## 10. 违章与隐患闭环管理

### 10.1 违章查处（`隐患违章管理-违章查处.html`）

**功能目标**：管理从AI预警、实时巡检、视频倒查和人工上报产生的违章记录，完成考核和处理。

**筛选字段**：发生时间、预警来源、违章名称、违章等级、违章单位、所属单位、涉及人员、处理状态、处理人、考核人。

**列表字段**：序号、发生时间、预警来源、违章名称、等级、违章单位、所属单位、违章人员、照片、考核人、考核时间、考核金额、记分、处理状态、处理人、处理时间、操作。涉及人员完整显示所有姓名，空间不足时换行或悬浮展示完整列表。

**状态流转**：待处理 → 处理中 → 已处理；可按业务配置增加已撤销。已处理记录只读，更正需发起更正流程。

**详情抽屉**

- “预警信息”：编号、时间、摄像头、人员、识别结果、置信度、等级、复核状态。
- “预警截图/视频”：可预览。
- “作业信息”：作业类型、分类、内容、区域、日期、单位、危险标识；区域旁提供“查看位置”按钮。
- “考核处理信息”：结论、金额、记分、说明、处理人和凭证。
- “复核结果”：展示每次复核记录与更正链。

### 10.2 隐患查处（`隐患违章管理-隐患查处.html`）

**功能目标**：管理隐患排查、整改、核实和验收闭环。

**字段**：发生时间、来源、分类、等级、位置、责任单位、描述、照片、排查人、整改状态、整改期限、整改人、整改时间、验收人、验收时间、验收结果。

**状态流转**：待核实 → 待整改 → 整改中 → 待验收 → 已闭环；核实为非隐患进入“非隐患”。验收不通过返回整改中并记录原因。

**超期规则**：当前时间超过整改期限且未闭环即为超期；超期天数按自然日差值计算。整改和验收均需上传凭证时，未上传不得提交。

### 10.3 违章规则管理（`违章规则管理.html`）

**功能目标**：将算法识别结果映射为违章名称、等级、条款和记分。

**列表字段**：序号、违章名称、违章类型、条款/依据、关联等级、扣分数、状态、操作。

**新增/编辑字段**

- 违章名称为下拉框，默认提示“请从算法中选择”。选项仅包含算法管理中启用且处置方式为“违章”的算法。
- 选择算法后保存 `algorithm_id`，名称默认取算法名称；同一算法仅允许一个有效违章规则。
- 违章类型、条款/依据、关联等级、扣分数均必填。扣分数为0～100的非负数。
- 算法停用时规则保留历史使用，但新预警不再触发。

### 10.4 违章等级管理（`违章等级管理.html`、`准则管理-违章等级管理.html`）

**字段**：序号、违章等级、违章条款、操作。当前列表不设置“违章类型”列。

**规则**：等级编码唯一，等级可配置颜色和排序；被规则引用的等级不可删除。兼容页面使用同一接口和字段定义。

### 10.5 违章类型管理（`违章类型管理.html`）

**字段**：类型名称、编码、上级类型、适用场景、状态、排序、说明。停用后历史正常展示，新建规则不可选。

### 10.6 隐患分类与等级（`隐患分类管理.html`、`隐患等级管理.html`）

**隐患分类字段**：分类名称、编码、上级分类、责任专业、默认整改期限、状态、排序。

**隐患等级字段**：等级名称、风险颜色、默认整改期限、是否要求升级审批、状态、排序。

**规则**：同层名称和编码唯一；重大隐患可以配置必须由上级组织验收；引用中的分类和等级只能停用。

## 11. 领导履职

### 11.1 领导履职台账（`领导履职台账.html`）

**功能目标**：统计领导或履职人员的视频倒查行为和发现问题情况。

**筛选字段**

| 字段 | 类型 | 规则 |
|---|---|---|
| 倒查日期 | 日期区间组件 | 默认今日，直接显示年月日开始和结束值 |
| 领导名称 | 人员搜索 | 仅匹配有履职记录且用户可查看的人员 |

**卡片**：倒查总视频数、参与人员数、倒查总时长、发现违章/隐患总数、整改完成率。卡片不展示环比。

**列表字段**：序号、倒查日期、履职人员、单位-部门、倒查总视频数、倒查时长、发现违章/隐患数、操作。

**日期与时长**：跨天记录显示日期范围；单日显示单日。时长统一显示“X天X时X分”。

### 11.2 领导履职详情（`领导履职详情.html`）

展示人员信息、倒查日期范围、倒查摄像头/录像清单、单次时长、标注问题和处置状态。总时长为明细秒数之和并转换为天时分；问题数按单据ID去重。点击问题进入有权限的业务详情。

## 12. 统计分析

### 12.1 违章统计分析（`违章统计分析.html`）

**数据范围**：用户组织权限内、发生时间落入统计区间的有效违章和复核数据。

**筛选字段**：统计日期、单位、部门、违章等级、违章类型、复核状态。

**指标口径**

- 违章总数：确认违章且未撤销的单据数。
- 已复核/待复核：按预警复核状态统计。
- 误报数：复核结果为非违章的预警数。
- 严重违章数：配置为严重等级的违章数。
- 有效违章率 = 确认违章数 ÷ 已复核预警数 × 100%。

**分析模块**：违章等级分布、预警来源分布、违章类型TOP10、复核结果分布、单位违章TOP10、违章人员TOP10、月度违章数量趋势、24小时发生时段分布、重复违章人员、误报类型分析。

**图表交互**：悬浮显示原始值与占比；点击图例切换系列；点击单位、类型或等级图形下钻列表；导出携带当前筛选条件和指标口径。

### 12.2 隐患统计分析（`隐患统计分析.html`）

**筛选字段**：统计日期、单位、部门、隐患等级、隐患分类、整改状态。

**指标口径**

- 隐患总数：核实为隐患且未撤销的单据数。
- 已整改数：进入待验收或已闭环的去重隐患数。
- 待整改数：状态为待整改。
- 整改中数：状态为整改中。
- 重大隐患数：等级为重大隐患。
- 整改完成率 = 已闭环隐患数 ÷ 应整改隐患数 × 100%。

**分析模块**：隐患等级分布、来源分布、隐患分类TOP10、整改状态分布、单位隐患TOP10、排查人员TOP10、月度隐患趋势、24小时发生时段分布、重复隐患点位、验收结果分布。

## 13. 算法、精准度与训练样本

### 13.1 算法管理（`算法管理.html`）

**功能目标**：维护算法元数据及其处置方式，为实时识别、规则管理和精准度统计提供主数据。

**筛选字段**：算法类型、处置方式、分析模型、名称/编码关键词。

**列表字段**：序号、算法名称、算法编码、算法类型、分析模型、算法处置方式、操作。

**显示规则**：分析模型只显示模型名称，不在名称后拼接V1.x版本；版本由算法版本数据单独管理。操作仅提供“编辑”。

**核心字段**：名称、编码、类型、分析模型、处置方式（违章/隐患/预警/仅识别）、启用状态、输入规格、输出标签、阈值、部署位置、负责人。编码唯一；修改处置方式需校验已关联规则。

### 13.2 算法精准度综合列表（`算法精准度分析列表.html`）

**功能目标**：按算法维度展示最新版本和综合精准度，不按单位拆分页面。

**顶部指标**：算法总数，为当前权限可查看的去重算法数量。

**列表字段**：序号、算法名称、算法类型、当前版本、综合精准度、更新时间、操作。列表不包含运行状态。

**综合精准度公式**

1. 对每个单位获取该算法最新一条“有效”精准度记录。
2. 缺失、未完成复核、分母为0或被标记无效的记录不参与。
3. 综合精准度 = 有效单位最新精准度之和 ÷ 有效单位数。
4. 四舍五入保留两位小数；无有效数据时显示“--”。

**导入**：点击“导入”上传算法精准度模板。模板字段为算法编码、单位编码、版本、统计时间、正确识别数、误报数、有效标识。系统校验算法、单位、版本、时间、重复键和数值合法性，预览确认后入库。

**详情**：携带 `algorithm=<算法编码>` 跳转详情；参数无法识别时展示无数据提示，不错误展示其他算法。

### 13.3 算法精准度详情（`算法精准度分析.html`）

**概览字段**：算法名称、编码、类型、当前版本、综合精准度、更新时间。

**精准度趋势**：只展示精准度一条曲线；支持近7天、近30天、近90天和自定义日期。横轴为统计日期，纵轴为百分比；切换区间后重新请求数据。

**训练记录字段**：训练时间、版本、正样本量、负样本量、精准度、提升幅度。训练记录不展示召回率和误报率。

**公式**：单次精准度按正确识别数 ÷（正确识别数 + 误报数）计算；提升幅度为本次有效精准度减上一次有效精准度。首个版本显示“--”。

### 13.4 训练样本库（`数据回溯正负样本.html`）

**菜单名称**：训练样本库。

**页签**：全部样本、正样本、负样本、视频正样本库。

**筛选字段**：样本类型、算法类型、来源、扫描时间、标签、组织。组织树默认收起，展开/收起按钮位于面板边界垂直居中。

**样本卡字段**：缩略图/视频、正负样本标识、业务标签、算法模型与版本、置信度、来源、采集时间、组织、复核人。

**置信度定义**：模型对当前预测类别的输出概率或归一化得分，仅表示模型判断强度，不等于准确率。不同模型分数不可直接横向比较，必须同时保存模型版本和阈值。

**样本规则**

- AI确认正确进入正样本；复核为误报进入负样本；人工审核后才进入可训练集合。
- 视频正样本保存有效片段开始/结束时间及原录像引用。
- 同一图片/片段按内容哈希去重；变更标签记录前后值和审核人。
- 导出训练样本需AI研发权限、脱敏和审计。

## 14. 算力智能调度

### 14.1 算力智能调度（`算力智能调度.html`）

**功能目标**：查看算法任务、边缘盒子/GPU节点负载并执行任务调度。

**字段**：节点名称、节点类型、组织、在线状态、GPU/CPU/内存使用率、队列长度、已部署模型、任务类型、优先级、开始时间、状态、失败原因。

**调度规则**

- 实时预警任务优先于事后倒查任务，人工上传演示任务可配置高优先级但不得抢占安全关键实时任务。
- 节点选择综合考虑模型可用性、资源余量、组织网络可达性和队列长度。
- 节点离线时，未开始任务重新排队；运行中任务按能力重试或标记失败。
- 手工重试、暂停和迁移需权限并记录日志。

## 15. 文档与方案辅助页面

### 15.1 产品功能说明页面（`产品功能说明.html`）

用于在线只读浏览本功能说明。支持目录定位、关键词搜索、章节折叠和打印，不提供在线修改业务规则的能力。

### 15.2 方案图册（`方案图册.html`）

用于按模块浏览原型截图和方案图。字段包括模块、页面名称、版本、更新时间和说明；点击图片支持放大、上一张/下一张。仅作为说明材料，不作为业务数据来源。

## 16. AI研发中心协作清单

### 16.1 实时监控能力

| 能力 | AI研发中心交付 | 业务研发配合 | 验收标准 |
|---|---|---|---|
| 视频接入 | 支持从海康平台/合肥院视频中台读取约10～20路工业场景视频 | 提供设备台账、鉴权、流地址映射和播放页面 | 指定视频可稳定取流，断流可检测和恢复 |
| 有人/无人识别 | 在本地防灾盒子部署人员入侵算法，并输出是否有人、时间、框坐标、置信度 | 维护作业与摄像头绑定，配置持续时长X和预警规则 | 有人开始后每30秒识别，持续至X分钟最后一次仍有人时触发 |
| 无作业人员停留 | 输出连续检测结果，不使用人员入侵算法原有报警规则 | 判断摄像头当前无作业绑定，触发预警并提升排序 | 有作业时不触发；无作业连续满足条件时仅触发一次有效预警 |
| 违章/隐患算法 | 选择3个适合演示的算法，输出类别、框坐标、置信度、截图 | 配置算法到违章/隐患规则和处置流程 | 画面显示红框及违章/隐患名称，预警可进入复核 |

### 16.2 视频倒查能力

| 能力 | AI研发中心交付 | 业务研发配合 | 验收标准 |
|---|---|---|---|
| NVR录像取流 | 支持按摄像头和作业时间从海康NVR取录像 | 作业结束触发任务、提供摄像头与时间范围 | 作业结束后自动创建取流任务，可追踪进度和失败原因 |
| 有人片段预计算 | 对回传录像运行人员算法，输出有人片段和时间轴 | 保存片段索引、关联作业并提供查询接口 | 打开倒查页面可直接定位预计算的有人片段 |
| 手工视频识别 | 支持约1分钟现场上传片段的人员识别 | 上传、格式校验、任务状态和结果展示 | 上传后能返回有人片段，失败可见原因并可重试 |
| 结果留存 | 提供结构化结果和版本信息 | 录像/识别结果按策略保存90天并定期清理 | 90天内可按作业、摄像头、时间查询；过期有审计记录 |

### 16.3 双方接口边界

AI侧统一输出任务ID、算法ID、模型版本、摄像头ID、原始时间戳、类别、置信度、检测框、截图/片段地址、处理状态和错误码。业务侧负责组织权限、作业关系、业务规则、状态流转、页面展示、消息通知、审计和数据保留策略。所有算法结果必须可追溯到模型版本与原始视频时间。

## 17. 异常与边界场景

1. 视频断流：宫格显示断流状态、最后在线时间和重试；不展示过期静态画面冒充实时视频。
2. 作业未绑定摄像头：发布时阻止或按配置进入无视频作业；运行中发现解绑立即告警。
3. 算法服务不可用：任务进入重试队列，页面展示处理中/失败；不得自动判定为无风险。
4. 重复预警：按摄像头、算法、对象和时间窗口合并，但保留原始事件计数。
5. 数据更正：已完成业务单据通过更正流程产生新版本，不覆盖历史记录。
6. 权限变化：用户权限被收回后立即不能访问新请求，已打开页面下一次请求返回无权提示。
7. 大数据量：列表、树和图表均由服务端分页或分层加载；导出走异步任务。
8. 多人并发：保存时校验版本号；冲突提示重新加载，避免覆盖他人修改。

## 18. 页面与章节覆盖表

| 页面文件 | 功能章节 |
|---|---|
| `index.html`、`首页.html` | 3.1 首页 |
| `监测一张图.html` | 3.2 监测一张图 |
| `作业计划管理.html` | 4.1 作业计划列表 |
| `新增作业计划.html` | 4.2 新增/编辑作业计划 |
| `新增作业计划-选择摄像头.html`、`选择摄像头.html` | 4.3 摄像头选择 |
| `无视频作业.html` | 4.4 无视频作业 |
| `作业区域管理.html` | 5.1 作业区域管理 |
| `作业班次.html` | 5.2 作业班次 |
| `作业类型管理.html` | 5.3 作业类型管理 |
| `设备管理列表.html` | 6.1 设备管理列表 |
| `设备管理树状.html` | 6.2 设备树状页 |
| `人脸采集库.html` | 6.3 人脸采集库 |
| `移动端人脸采集.html` | 6.4 移动端人脸采集 |
| `实时监控.html` | 7.1 实时监控 |
| `实时在线监控轮播规则弹窗.html` | 7.2 自动播放设置 |
| `AI预警实时和倒查标记.html` | 7.3 实时抓拍与人工标注 |
| `视频倒查列表页面.html` | 8.1 视频倒查列表 |
| `视频倒查按作业计划.html` | 8.2 按作业计划倒查 |
| `AI隐患核查.html` | 9.1 AI预警复核列表 |
| `AI识别的违章复核.html` | 9.2 AI预警复核详情 |
| `隐患违章管理-违章查处.html` | 10.1 违章查处 |
| `隐患违章管理-隐患查处.html` | 10.2 隐患查处 |
| `违章规则管理.html` | 10.3 违章规则管理 |
| `违章等级管理.html`、`准则管理-违章等级管理.html` | 10.4 违章等级管理 |
| `违章类型管理.html` | 10.5 违章类型管理 |
| `隐患分类管理.html`、`隐患等级管理.html` | 10.6 隐患分类与等级 |
| `领导履职台账.html` | 11.1 领导履职台账 |
| `领导履职详情.html` | 11.2 领导履职详情 |
| `违章统计分析.html` | 12.1 违章统计分析 |
| `隐患统计分析.html` | 12.2 隐患统计分析 |
| `算法管理.html` | 13.1 算法管理 |
| `算法精准度分析列表.html` | 13.2 精准度综合列表 |
| `算法精准度分析.html` | 13.3 精准度详情 |
| `数据回溯正负样本.html` | 13.4 训练样本库 |
| `算力智能调度.html` | 14.1 算力智能调度 |
| `产品功能说明.html` | 15.1 产品功能说明页面 |
| `方案图册.html` | 15.2 方案图册 |

## 19. 开发与验收总则

1. 每个字段均需实现后端校验、前端提示、权限校验和审计要求；不能只依赖页面必填星号。
2. 所有统计指标必须能通过明细下钻复算，图表总数与同筛选条件列表保持一致。
3. 所有状态流转需覆盖正常、驳回、撤销、重复提交、并发修改和无权限场景。
4. 所有文件上传需校验扩展名、MIME、大小、病毒风险和业务归属，禁止使用原始文件名直接构造公开地址。
5. 所有异步任务需展示等待、进行中、成功、部分成功和失败状态，并提供失败原因与可重试入口。
6. 核心闭环验收至少覆盖：作业发布与视频绑定、实时预警、人工标注、AI复核、违章/隐患处置、样本沉淀、精准度更新和统计复算。
